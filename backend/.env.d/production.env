# ── Production Environment ────────────────────────
NODE_ENV=production
PORT=4000
LOG_LEVEL=warn

# Strict auth (keys set via secrets manager)
AUTH_ENABLED=true
# API_KEYS=<set via secrets manager / GitHub Secrets / Vault>

# Strict rate limits
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_GLOBAL=100
RATE_LIMIT_MAX_EVALUATE=5

# Data: minimal storage, auto-redact
DATA_STORE_CASE_TEXT=false
DATA_RETENTION_DAYS=90
DATA_AUTO_REDACT_PII=true
DATA_PURGE_INTERVAL_MS=3600000

# Scoring: production weights
SCORE_WEIGHT_FEASIBILITY=0.45
SCORE_WEIGHT_INNOVATION=0.35
SCORE_WEIGHT_RISK=0.20
SCORE_THRESHOLD_SHIP=75
SCORE_THRESHOLD_ITERATE=50
SCORE_RISK_INVERSION=true

# AI: dual-pass for reliability
AI_DUAL_PASS=true
AI_TEMPERATURE=0.2

# CORS: production frontend domain only
# CORS_ORIGINS=https://app.example.com

# ─── Secrets (NEVER commit real values) ──────────
# Set these via your secrets manager:
#   LLM_API_KEY
#   LLM_FALLBACK_API_KEY
#   ATTESTATION_PRIVATE_KEY
#   API_KEYS
